namespace Pelco.PDK.Metadata
{
    /// <summary>
    /// Defines the playing state.
    /// </summary>
    public enum PlayingState
    {
        NONE,
        PLAYING,
        PAUSED,
        STOPPED,
        INITIALIZED,
    }
}
